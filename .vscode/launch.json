{
    "version": "0.2.0",
    "configurations": [
    
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Open active file (Chrome)",
            "file": "${file}",
            "cwd": "${workspaceFolder}",
            "webRoot": "${workspaceFolder}"
        }
    ]
}